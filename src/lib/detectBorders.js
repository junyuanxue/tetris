import constants from '../constants/constants'

const hasCollision = (direction) => {
  switch (direction) {
    case 'left':

      break

    case 'right':
      break

    case 'left':
      break

    default:
      break
  }
}

export default hasCollision
